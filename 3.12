# Из массива размером 12 удалить все отрицательные элементы
print('lab3 3 уровень 12 задание')
M = 12
A = [0]*12
for i in range(M):
    A[i] = int(input('Введите число массива'))
B = [i for i in A if i >= 0]
print('Исходный массив:', A)
print('Искомый массив:', B)
